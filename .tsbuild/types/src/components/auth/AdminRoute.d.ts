import { type ReactNode } from "react";
interface AdminRouteProps {
    children: ReactNode;
}
export declare function AdminRoute({ children }: AdminRouteProps): import("react").JSX.Element;
export {};
