# Production Root Cause Register — 2026-09-02

| ID | Severity | Problem | Root Cause | Component | Fix | Validation | Status |
|---|---|---|---|---|---|---|---|
| RC-01 | CRITICAL | Frontend API calls fail in production (all catalog/cart/checkout broken) | `VITE_PUBLIC_API_URL` not set at build time, defaults to `http://localhost:8788` via `packages/config/src/env.ts` zod default | `apps/customer/src/lib/config.ts` + build | Added production fallback to `https://nabome-api.pages.dev` with runtime hostname check; rebuilt with `VITE_PUBLIC_API_URL` explicit | `curl` bundle grep shows production URL; `typecheck` pass; `https://nabome-api.pages.dev/api/v1/products` 200 | VERIFIED |
| RC-02 | CRITICAL | CORS blocks `www.nabome.online` even after fixing API URL | `apps/api/wrangler.jsonc` production `vars` had no `CORS_ORIGINS`; `allowedOrigins()` returned `[]`, so `applyCors` never set header. Cloudflare added `*` which fails with `credentials:include` | `apps/api/_lib/security.ts` + `wrangler.jsonc` | Added `DEFAULT_ALLOWED_ORIGINS` fallback (nabome.online, www, pages.dev, localhost) in `security.ts`; updated `wrangler.jsonc` PUBLIC_API_URL to `nabome-api.pages.dev` | `curl -H Origin:https://www.nabome.online` now returns `access-control-allow-origin: https://www.nabome.online` with credentials; evil.com returns no CORS header | VERIFIED |
| RC-03 | CRITICAL | `https://api.nabome.online` NXDOMAIN — documented production API URL unreachable | DNS for `api.nabome.online` never provisioned; `PUBLIC_API_URL` in wrangler pointed to non-existent host | DNS / `wrangler.jsonc` | Changed `PUBLIC_API_URL` to `https://nabome-api.pages.dev` (existing Pages domain) until custom domain provisioned | `dig api.nabome.online` NXDOMAIN confirmed; `nabome-api.pages.dev/health` 200 | VERIFIED |
| RC-04 | HIGH | API production deploy broken (health 404) after local wrangler deploy | `wrangler.jsonc` `triggers.crons` invalid for Pages Functions; `wrangler 4.120` validation failed, `4.103` deployed broken bundle. Also `CORS_ORIGINS` var conflicted with binding name | `apps/api/wrangler.jsonc` | Removed `triggers` (Pages cron needs separate handling), removed `CORS_ORIGINS` var from wrangler (now defaulted in code), deployed via `pnpm --filter @nabome/api exec wrangler` | `curl https://nabome-api.pages.dev/health` 200 after deploy; `wrangler pages deploy` exit 0 | VERIFIED |
| RC-05 | MEDIUM | `triggers.crons` settlement job not running after fix | Cron removed to unblock deploy; no scheduled settlement | Infra | Documented as remaining risk; needs Cloudflare Workers Cron or Pages scheduled function | N/A | BLOCKED - needs infra decision |
| RC-06 | INFO | CSP `connect-src 'self' https:` overly broad but works for pages.dev | Not a bug — allows any https, but should be tightened to specific API origin | `apps/customer/index.html` | No change needed now; works with `nabome-api.pages.dev` | Verified `connect-src https:` allows pages.dev | VERIFIED |
